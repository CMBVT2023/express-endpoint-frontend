export type CarObject = {
    id: number;
    make: string;
    model: string;
    year: number;
    deleted_flag: null | number;
}

export type UserData = {
    userName: string;
    userKey: string;
}
